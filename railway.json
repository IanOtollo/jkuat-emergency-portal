{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "bash build.sh"
    },
    "deploy": {
        "startCommand": "python backend/manage.py migrate && python backend/manage.py seed_data && gunicorn --pythonpath backend incident_system.wsgi",
        "restartPolicyType": "ON_FAILURE"
    }
}