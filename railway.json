{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "bash build.sh"
    },
    "deploy": {
        "startCommand": "python3 backend/manage.py migrate && python3 backend/manage.py seed_data && gunicorn --pythonpath backend --bind 0.0.0.0:$PORT --timeout 600 incident_system.wsgi",
        "restartPolicyType": "ON_FAILURE"
    }
}