{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "bash build.sh"
    },
    "deploy": {
        "startCommand": "gunicorn --pythonpath backend incident_system.wsgi",
        "restartPolicyType": "ON_FAILURE"
    }
}